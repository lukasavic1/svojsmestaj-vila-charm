export const siteConfig = {
  name: "Villa Charm",
  /** Production property site. */
  isDemo: false,
  /** Default origin (www). villacharm.rs is an equal live host — see lib/site-hosts.ts. */
  url: "https://www.villa-charm.com",
  defaultOgImage: "/images/exterior-1.jpg",
} as const;
